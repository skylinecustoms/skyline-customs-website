/*
 * SKYLINE CUSTOMS — Local SEO Landing Page
 * Ceramic Coating in Vienna, VA
 * URL: /ceramic-coating-vienna-va
 * Content comes from client/src/lib/localSeo.ts via the shared LocalServicePage template.
 */
import LocalServicePage from "@/components/LocalServicePage";

export default function ViennaCeramic() {
  return <LocalServicePage city="Vienna" service="ceramic" />;
}
